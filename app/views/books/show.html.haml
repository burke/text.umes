%h1=h @book.title
%hr/

%table{:style => 'width:100%;'}
  %tr
    %td.a
      %span.title=h @book.title
      %span.isbn== (#{h @book.isbn})
      %br/
      %span.author=h @book.author
    %td.b
      %span.price= number_to_currency @book.price
      %br/
      %span.seller=h @book.seller
    %td.c= image_tag @book.thumbnail_url

%div.comments
  %p
    %strong Seller's Comments:
  %p=h @book.comments

- form_tag :action => 'contact', :id => @book.id do |f|
  %span.message
    = text_area_tag :message, "I'd like to buy this book. I'm available at ______ tomorrow and can meet you ____. Does this work?"
    %label{ :for => 'buyer' } Your UMNetID (eg. umsample42)
    <input id="buyer" name="buyer" type="text" class="fancy" />
    %p
      %br/
      = submit_tag "Contact Seller"


